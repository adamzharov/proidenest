/**
 * Parses the scale configuration from the provided data.
 * This function should be called before `parseData` to ensure
 * the scales are set up correctly based on the initial data.
 *
 * @param {Object} data - The raw data object containing scale configurations.
 * @returns {Object} - The parsed scale configuration.
 *
 * Example usage:
 * 